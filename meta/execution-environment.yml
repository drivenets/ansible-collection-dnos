---
version: 3

dependencies:
  # Python dependencies required at runtime on the controller
  python_interpreter:
    package_system: "python3.11"
  
  python: requirements.txt
  
  # System-level packages (OS dependencies)
  system: bindep.txt

build_arg_defaults:
  ANSIBLE_GALAXY_CLI_COLLECTION_OPTS: '--pre'
